import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Update this import

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
